// The data to parse for day 05 will be used
// to explore the nom crate for data parsing.

struct Almanac {}

impl Almanac {
    fn from_file(fname: &str) -> Self {
        Self {}
    }
}
#[test]
fn test_parse_input() {
    let almanac = Almanac::from_file("resources/day05_input.txt");
}
